import Tgrad.Growth.BroadcastAddManifest

/- GENERATED by scripts/spec/generate_broadcast_add_manifest.py; do not edit. -/

namespace Tgrad.Growth.BroadcastAddManifestGenerated

open Tgrad.Requirements
open Tgrad.Growth
open Tgrad.Growth.BroadcastAddManifest

def manifest : FrozenManifest :=
  { schemaVersion := 1
    packetId := "WORK-SPEC-BROADCAST-ADD-PROSPECTIVE-V1"
    baselineRevision := "51244a9"
    upstreamRevision := "19c4d736f2bc8e26d21f08b28ffd6298408da00f"
    backend := "METAL"
    deterministic := true
    operator := "Tensor.__add__"
    legalTrace := [.constructLeft, .constructRight, .snapshotInputs, .invokeAdd, .captureResultIdentity, .observeShape, .observeDtype, .realize1, .readback1, .realize2, .readback2, .snapshotInputsAfter]
    incompatibleTrace := [.constructLeft, .constructRight, .invokeAdd, .observeShapeIfConstructed, .recordTerminalStageClassMessage]
    exclusions := ["autograd", "reverse-add API surface", "symbolic dimensions", "zero-size dimensions", "broad dtype lattice", "performance", "concurrency"]
    path := "fixtures/requirements/broadcast_add_prospective_v1.json"
    contentHash := "a0485899ccae42c6378bb96f1fafec802c73c31aa34d956fda9b7f7deb9ec2e9"
    requirementIds := [⟨"REQ-ADD-LEGAL-SAME-DTYPE"⟩, ⟨"REQ-ADD-DTYPE-PROMOTION"⟩, ⟨"REQ-ADD-INCOMPATIBLE-SHAPE"⟩, ⟨"REQ-REALIZE-IDEMPOTENT"⟩]
    scenarios := [{ id := "ADD-SAME-SHAPE-F32", requirementIds := [⟨"REQ-ADD-LEGAL-SAME-DTYPE"⟩, ⟨"REQ-REALIZE-IDEMPOTENT"⟩], left := { kind := .tensor, construction := "typed_tensor", shape := [2, 3], dtype := .float32, valueCount := 6 }, right := { kind := .tensor, construction := "typed_tensor", shape := [2, 3], dtype := .float32, valueCount := 6 }, observations := [.construction, .shape, .dtype, .exactValues, .realizeIdentity, .repeatedReadback, .inputsUnchanged] }, { id := "ADD-SINGLETON-AXIS-F32", requirementIds := [⟨"REQ-ADD-LEGAL-SAME-DTYPE"⟩, ⟨"REQ-REALIZE-IDEMPOTENT"⟩], left := { kind := .tensor, construction := "typed_tensor", shape := [2, 3], dtype := .float32, valueCount := 6 }, right := { kind := .tensor, construction := "typed_tensor", shape := [2, 1], dtype := .float32, valueCount := 2 }, observations := [.construction, .shape, .dtype, .exactValues, .realizeIdentity, .repeatedReadback, .inputsUnchanged] }, { id := "ADD-RANK-EXTENSION-I32", requirementIds := [⟨"REQ-ADD-LEGAL-SAME-DTYPE"⟩, ⟨"REQ-REALIZE-IDEMPOTENT"⟩], left := { kind := .tensor, construction := "typed_tensor", shape := [2, 3], dtype := .int32, valueCount := 6 }, right := { kind := .tensor, construction := "typed_tensor", shape := [3], dtype := .int32, valueCount := 3 }, observations := [.construction, .shape, .dtype, .exactValues, .realizeIdentity, .repeatedReadback, .inputsUnchanged] }, { id := "ADD-TWO-SIDED-BROADCAST-F32", requirementIds := [⟨"REQ-ADD-LEGAL-SAME-DTYPE"⟩, ⟨"REQ-REALIZE-IDEMPOTENT"⟩], left := { kind := .tensor, construction := "typed_tensor", shape := [2, 1, 3], dtype := .float32, valueCount := 6 }, right := { kind := .tensor, construction := "typed_tensor", shape := [1, 2, 1], dtype := .float32, valueCount := 2 }, observations := [.construction, .shape, .dtype, .exactValues, .realizeIdentity, .repeatedReadback, .inputsUnchanged] }, { id := "ADD-I32-F32-SCALAR-PROMOTION", requirementIds := [⟨"REQ-ADD-DTYPE-PROMOTION"⟩, ⟨"REQ-REALIZE-IDEMPOTENT"⟩], left := { kind := .tensor, construction := "typed_tensor", shape := [2, 3], dtype := .int32, valueCount := 6 }, right := { kind := .tensor, construction := "typed_zero_dimensional_tensor", shape := [], dtype := .float32, valueCount := 1 }, observations := [.construction, .shape, .dtype, .exactValues, .realizeIdentity, .repeatedReadback, .inputsUnchanged] }, { id := "ADD-INCOMPATIBLE-SHAPES", requirementIds := [⟨"REQ-ADD-INCOMPATIBLE-SHAPE"⟩], left := { kind := .tensor, construction := "typed_tensor", shape := [2, 3], dtype := .float32, valueCount := 6 }, right := { kind := .tensor, construction := "typed_tensor", shape := [2, 2], dtype := .float32, valueCount := 4 }, observations := [.construction, .shapeAccess, .exceptionStage, .exceptionClass, .exceptionMessage] }]
    mutations := [{ id := "MUT-ADD-SUBTRACT", targetScenarios := ["ADD-SAME-SHAPE-F32"], fault := "replace addition with subtraction", implementation := "observer-owned substitution of the add opcode with sub", mustFail := [.value], mustNotChange := [.shape, .dtype, .terminalOutcome], mayBeUnobserved := [] }, { id := "MUT-ADD-WRONG-RIGHT-ALIGNMENT", targetScenarios := ["ADD-RANK-EXTENSION-I32"], fault := "align the rank-one operand from the left", implementation := "observer-owned broadcast alignment mutant", mustFail := [.terminalOutcome], mustNotChange := [.dtype], mayBeUnobserved := [.shape, .value] }, { id := "MUT-ADD-NO-SINGLETON-EXPANSION", targetScenarios := ["ADD-SINGLETON-AXIS-F32"], fault := "reject expansion of a size-one axis", implementation := "observer-owned singleton-compatibility mutant", mustFail := [.terminalOutcome], mustNotChange := [.dtype], mayBeUnobserved := [.shape, .value] }, { id := "MUT-ADD-WRONG-PROMOTION", targetScenarios := ["ADD-I32-F32-SCALAR-PROMOTION"], fault := "retain int32 for int32 plus float32", implementation := "observer-owned promotion-result mutant", mustFail := [.dtype, .value], mustNotChange := [.shape, .terminalOutcome], mayBeUnobserved := [] }, { id := "MUT-ADD-ACCEPT-INCOMPATIBLE", targetScenarios := ["ADD-INCOMPATIBLE-SHAPES"], fault := "accept the incompatible shape pair", implementation := "observer-owned compatibility predicate mutant", mustFail := [.exceptionStage, .exceptionClass, .exceptionMessage], mustNotChange := [], mayBeUnobserved := [] }, { id := "MUT-REALIZE-RETURNS-COPY", targetScenarios := ["ADD-SAME-SHAPE-F32"], fault := "return an observationally equal distinct Tensor object from realize", implementation := "observer-owned result-identity mutant", mustFail := [.objectIdentity], mustNotChange := [.shape, .dtype, .value, .readbackStability, .inputImmutability], mayBeUnobserved := [] }, { id := "MUT-REALIZE-SECOND-READBACK", targetScenarios := ["ADD-SAME-SHAPE-F32"], fault := "alter only the second readback", implementation := "observer-owned second-readback mutant", mustFail := [.readbackStability], mustNotChange := [.shape, .dtype, .objectIdentity, .inputImmutability], mayBeUnobserved := [] }, { id := "MUT-ADD-MUTATES-LEFT", targetScenarios := ["ADD-SAME-SHAPE-F32"], fault := "mutate only the left input after add", implementation := "observer-owned post-call input mutant", mustFail := [.inputImmutability], mustNotChange := [.shape, .dtype, .value, .objectIdentity, .readbackStability], mayBeUnobserved := [] }]
    expectedBefore := .unobserved
    allowedAfter := [.failed, .passedCalibrated, .blocked, .verifierError]
    forbiddenInferences := [.conformant, .promoted, .fullTinygradParity] }

theorem manifest_is_well_formed : manifest.wellFormed := by native_decide
theorem manifest_has_six_scenarios_and_eight_atomic_mutations :
    manifest.scenarios.length = 6 ∧ manifest.mutations.length = 8 := by native_decide

end Tgrad.Growth.BroadcastAddManifestGenerated
